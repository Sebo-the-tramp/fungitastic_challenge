\begin{abstract}
% Fine-grained semantic segmentation is inherently challenging, as it combines the difficulties of precise localization with subtle inter-class distinctions. This challenge is further exacerbated by the highly imbalanced class distribution of the FungiTastic dataset. A straightforward approach is to leverage SAM3 by prompting it with specific fungal classes; however, this method suffers from limited scalability, slow inference, and low IoU due to frequent class omissions. Moreover, its computational cost grows with both the number of images and the number of classes.

% To address these limitations, we propose a two-stage framework. First, SAM3 is used to generate class-agnostic segments. Then, a foundation model (DINOv3) extracts latent representations for each segment, enabling classification in embedding space. This decoupling of segmentation and classification improves scalability and flexibility. We demonstrate that our approach is effective across one-shot, few-shot, and full-data regimes, maintaining strong performance across varying levels of supervision.

% \todo{Furthermore, we show that we can leverage the segments to perform augmentations on the few-shot examples and combine them to further boost performances! -> IF TRUE}

% The code will be publicly released upon acceptance.
Fine-grained semantic segmentation requires both precise localization and discrimination between visually similar classes. In FungiTastic, this problem is further complicated by a long-tailed distribution and strong variation in image acquisition conditions.

We propose a parameter-free two-stage framework that decouples segmentation from classification. SAM3 first produces class-agnostic mushroom masks using macro-taxonomic prompts, and DINOv3 then assigns fine-grained labels through prototype matching in the embedding space. To improve this stage, we apply a simple transformation of the DINOv3 feature space that improves prototype-based classification.

Compared with class-specific prompting, our approach is more scalable, and keeps segmentation cost low. We report results from one-shot to few-hundred-shot regimes, providing, to the best of our knowledge, the first baseline for fine-grained semantic segmentation in low-data settings.

\end{abstract}